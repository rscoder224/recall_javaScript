// change text
// textContent property and innerText property
// textContent - will get all the text including hidden text
// innerText - will get only visible text
const element = document.getElementById('main-heading');

// element.textContent = 'This is something else';

// console.log(element);

// element.innerText = 'This is Mobile devices';

